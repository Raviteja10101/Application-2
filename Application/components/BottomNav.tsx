import {usePathname,useRouter} from 'expo-router'; import {View,Text,Pressable,StyleSheet} from 'react-native'; import {MaterialIcons} from '@expo/vector-icons'; import {colors} from '../theme';
const items=[{label:'Ledger',icon:'menu-book',path:'/'},{label:'Focus',icon:'explore',path:'/focus'},{label:'Session Diary',icon:'history-edu',path:'/diary'}] as const;
export default function BottomNav(){const router=useRouter(),pathname=usePathname();return <View style={styles.bar}>{items.map(i=>{const activeNow=pathname===i.path;return <Pressable key={i.path} style={[styles.item,activeNow&&styles.active]} onPress={()=>router.push(i.path)}><MaterialIcons name={i.icon as any} size={22} color={activeNow?colors.brass:colors.muted}/><Text style={[styles.label,{color:activeNow?colors.brass:colors.muted}]}>{i.label}</Text></Pressable>})}</View>}
const styles=StyleSheet.create({bar:{position:'absolute',left:0,right:0,bottom:0,height:74,backgroundColor:colors.surface,borderTopWidth:1,borderTopColor:colors.border,flexDirection:'row',justifyContent:'space-around',alignItems:'center'},item:{minWidth:90,alignItems:'center',padding:8,borderRadius:8},active:{backgroundColor:colors.surfaceLow},label:{fontSize:11,marginTop:3,fontWeight:'600'}});